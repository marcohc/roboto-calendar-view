buildscript {

    apply from: 'config/dependencies.gradle'

    repositories {
        mavenCentral()
        google()
    }

    dependencies {
        classpath gradlePlugins.gradle
    }
}

allprojects {
    repositories {
        mavenCentral()
        google()
    }
}
